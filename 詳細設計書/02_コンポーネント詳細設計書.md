# ST管理システム コンポーネント詳細設計書

**版数**: 1.0
**作成日**: 2026/05/27
**ベース**: 基本設計書 設計書概説 / 画面一覧

---

## 1. フロントエンドコンポーネント設計

### 1.1 コンポーネントツリー

```
<App>
  <I18nextProvider>
    <AuthProvider>
      <BrowserRouter>
        <Routes>
          <Route path="/login" element={<Login />} />
          <Route path="/" element={<ProtectedRoute><Layout /></ProtectedRoute>}>
            <Route index element={<Navigate to="/employee" />} />
            <Route path="employee" element={<Employee />} />
            <Route path="customer" element={<Customer />} />
            <Route path="attendance" element={<Attendance />} />
            <Route path="invoice" element={<Invoice />} />
          </Route>
          <Route path="*" element={<NotFound />} />
        </Routes>
      </BrowserRouter>
    </AuthProvider>
  </I18nextProvider>
</App>
```

### 1.2 各コンポーネント詳細設計

#### 1.2.1 Layout.jsx — メインレイアウト

| 項目 | 内容 |
|------|------|
| ファイル | `frontend/src/components/Layout.jsx` |
| 種別 | 共通コンポーネント |
| 親 | App.jsx (Route `/` の子) |

**Props**: なし（Context から認証情報取得）

**内部状態**:

| 状態 | 型 | 初期値 | 説明 |
|------|------|--------|------|
| collapsed | boolean | false | サイドバー折りたたみ (未使用、将来拡張用) |

**使用するContext**:
- `AuthContext`: user, logout

**レンダリング要素**:

```
<Layout style={{ minHeight: '100vh' }}>
  <Sider width={220} theme="dark">
    <div className="logo-area">  {/* ロゴ + システム名 */}
    <Menu theme="dark" selectedKeys={[currentPath]} items={menuItems}>
      {/* 4メニュー項目: 社員/顧客/勤務/請求書 */}
  <Layout>
    <Header style={{ background: '#fff' }}>
      <Space>  {/* ユーザー名 + ログアウトボタン */}
    <Content style={{ padding: 24 }}>
      <Outlet />  {/* React Router */}
```

**イベントハンドリング**:

| イベント | トリガー | 処理 |
|----------|---------|------|
| onMenuClick | Menu.Item クリック | navigate(path) で該当ページへ遷移 |
| onLogout | ログアウトボタン クリック | auth.logout() → navigate('/login') |

**使用するAnt Designコンポーネント**:
`Layout`, `Layout.Sider`, `Layout.Header`, `Layout.Content`, `Menu`, `Button`, `Space`, `Typography.Text`

---

#### 1.2.2 Login.jsx — ログイン画面

| 項目 | 内容 |
|------|------|
| ファイル | `frontend/src/pages/Login.jsx` |

**内部状態**:

| 状態 | 型 | 初期値 | 説明 |
|------|------|--------|------|
| username | string | '' | ユーザー名入力値 |
| password | string | '' | パスワード入力値 |
| loading | boolean | false | ログイン処理中フラグ |
| errorMsg | string | '' | エラーメッセージ（空=非表示） |

**フォームバリデーション**:

| 項目 | ルール | エラーメッセージ(ja/zh) |
|------|--------|------------------------|
| username | 必須 | ユーザー名を入力してください / 请输入用户名 |
| password | 必須 | パスワードを入力してください / 请输入密码 |

**イベントハンドリング**:

| イベント | 処理 |
|----------|------|
| onSubmit | バリデーション → loading=true → auth.login(username, password) → 成功時 navigate('/employee') 、失敗時 errorMsg設定 |
| onChange | 入力値の状態更新 + エラーメッセージクリア |

**画面レイアウトキーCSS**:
- 背景: `linear-gradient(135deg, #667eea 0%, #764ba2 100%)`
- カード: width 400px, border-radius 8px, 中央配置 (flexbox)

**使用するAnt Designコンポーネント**:
`Form`, `Input`, `Input.Password`, `Button`, `Alert`, `Card`, `Typography`

---

#### 1.2.3 Employee.jsx — 社員管理画面

| 項目 | 内容 |
|------|------|
| ファイル | `frontend/src/pages/Employee.jsx` |

**内部状態**:

| 状態 | 型 | 初期値 | 説明 |
|------|------|--------|------|
| data[] | EmployeeDTO[] | [] | 一覧データ |
| loading | boolean | false | ローディング状態 |
| pagination | object | {page:0, size:10, total:0} | ページネーション |
| keywordCode | string | '' | 社員番号検索キーワード |
| keywordName | string | '' | 氏名検索キーワード |
| modalVisible | boolean | false | モーダル表示 |
| modalMode | 'create'\|'edit' | 'create' | モーダルモード |
| selectedRecord | EmployeeDTO\|null | null | 編集中レコード |
| formLoading | boolean | false | フォーム保存中 |

**データフェッチ**:

```javascript
// 初回表示 + pagination.page/pagination.size 変更時
useEffect(() => {
  fetchEmployees();
}, [pagination.page, pagination.size]);

const fetchEmployees = async () => {
  setLoading(true);
  try {
    const res = await api.get('/employee', {
      params: { keyword: keyword, page: pagination.page, size: pagination.size }
    });
    setData(res.data.data.content);
    setPagination(prev => ({ ...prev, total: res.data.data.totalElements }));
  } finally {
    setLoading(false);
  }
};
```

**検索処理** (debounce 300ms):

```javascript
const handleSearch = useCallback(debounce((value) => {
  setKeyword(value);
  setPagination(prev => ({ ...prev, page: 0 })); // 1ページ目にリセット
}, 300), []);
```

**テーブルカラム定義**:

| カラムキー | title | dataIndex | width | render |
|-----------|-------|-----------|-------|--------|
| employeeCode | 社員番号 | employeeCode | 120 | テキスト |
| name | 氏名 | name | 120 | テキスト |
| department | 部署 | department | 100 | テキスト |
| email | メール | email | 220 | テキスト |
| phone | 電話番号 | phone | 140 | テキスト |
| actions | 操作 | - | 140 | `<Button type="link">編集</Button> <Button type="link" danger>削除</Button>` |

**モーダルフォームフィールド定義** (3セクション、13フィールド):

```
個人情報セクション:
├── employeeCode (Form.Item: Input, 必須, maxLength=20)
├── name (Form.Item: Input, 必須, maxLength=100)
├── email (Form.Item: Input type=email, maxLength=255)
├── phone (Form.Item: Input, maxLength=20)
├── address (Form.Item: Input, maxLength=500, fullWidth)
├── department (Form.Item: Select, options=[営業部,技術部,経理部,人事部])
└── position (Form.Item: Select, options=[部長,課長,係長,一般社員])

銀行口座情報セクション:
├── bankName (Form.Item: Input, maxLength=200)
├── bankBranch (Form.Item: Input, maxLength=200)
├── bankAccountType (Form.Item: Select, options=[普通,当座])
├── bankAccountNumber (Form.Item: Input, maxLength=50)
└── bankAccountHolder (Form.Item: Input, maxLength=100, fullWidth)

添付ファイルセクション:
└── attachment (Form.Item: Upload, accept=".pdf,.doc,.docx,.jpg,.png", maxCount=1)
    → 既存ファイル表示: fileName + ダウンロードリンク + 削除ボタン
```

**登録/更新処理**:

```javascript
const handleSubmit = async (values) => {
  setFormLoading(true);
  try {
    if (modalMode === 'create') {
      await api.post('/employee', values);
    } else {
      await api.put(`/employee/${selectedRecord.id}`, values);
    }
    setModalVisible(false);
    fetchEmployees(); // 一覧再取得
    message.success(t('保存しました'));
  } catch (err) {
    message.error(err.response?.data?.error || t('保存に失敗しました'));
  } finally {
    setFormLoading(false);
  }
};
```

**削除処理** (確認ダイアログ):

```javascript
const handleDelete = (record) => {
  Modal.confirm({
    title: '削除確認',
    content: `${record.name} を削除しますか？`,
    onOk: async () => {
      await api.delete(`/employee/${record.id}`);
      await fetchEmployees();
      message.success('削除しました');
    }
  });
};
```

---

#### 1.2.4 Customer.jsx — 顧客管理画面

| 項目 | 内容 |
|------|------|
| ファイル | `frontend/src/pages/Customer.jsx` |

**Employee.jsx との差分**:

| 項目 | Employee.jsx | Customer.jsx |
|------|-------------|-------------|
| APIベースパス | /api/employee | /api/customer |
| 検索フィールド | 社員番号, 氏名 | 顧客番号, 会社名 |
| カラム1 | employeeCode (社員番号) | customerCode (顧客番号) |
| カラム2 | name (氏名) | companyName (会社名) |
| カラム3 | department (部署) | contactName (担当者名) |
| モーダルセクション1 | 個人情報 (7項目) | 会社情報 (6項目: 部署/役職なし) |
| 添付ファイル | あり | なし |
| モーダル幅 | 640px | 640px |

---

#### 1.2.5 Attendance.jsx — 勤務管理画面

| 項目 | 内容 |
|------|------|
| ファイル | `frontend/src/pages/Attendance.jsx` |

**フィルター用追加状態**:

| 状態 | 型 | 初期値 | 説明 |
|------|------|--------|------|
| filterYear | number | currentYear | 年度フィルター |
| filterMonth | number | currentMonth | 月度フィルター |
| filterEmployeeId | number\|null | null | 社員フィルター (null=全社員) |
| employees[] | {id, name}[] | [] | 社員ドロップダウン用データ |

**テーブルカラム定義 (7列)**:

| カラムキー | dataIndex | render |
|-----------|-----------|--------|
| workDate | workDate | dayjs(date).format('YYYY/MM/DD') |
| employeeName | employeeName | テキスト |
| workHours | workHours | 小数点1桁 |
| overtimeHours | overtimeHours | 小数点1桁 |
| status | status | `<Tag>` (出勤=green, 欠勤=red, 休暇=blue) |
| remark | remark | テキスト |
| actions | - | 編集/削除リンク |

**「CSVエクスポート」ボタン**:

```javascript
const handleExport = async () => {
  const res = await api.get('/attendance/export', {
    params: { year: filterYear, month: filterMonth, employeeId: filterEmployeeId },
    responseType: 'blob'
  });
  const url = window.URL.createObjectURL(new Blob([res.data]));
  const link = document.createElement('a');
  link.href = url;
  link.setAttribute('download', `勤務記録_${filterYear}${filterMonth}.csv`);
  link.click();
};
```

**勤務時間バリデーション**:

| フィールド | ルール |
|-----------|--------|
| workHours | 必須, 0〜24, 0.5刻み |
| overtimeHours | 0〜12, 0.5刻み |
| status | 必須, enum[出勤,欠勤,休暇] |

---

#### 1.2.6 Invoice.jsx — 請求書・注文書管理画面

| 項目 | 内容 |
|------|------|
| ファイル | `frontend/src/pages/Invoice.jsx` |

**2層構造の設計**:

```
┌─ 上層カード: 請求書管理 ──────────────────────┐
│  フィルターバー (年度/月度/顧客 + 新規登録ボタン)  │
│  請求書テーブル (7列)                           │
│  ページネーション                                │
└────────────────────────────────────────────────┘
┌─ 下層カード: 請求書詳細 (選択時のみ表示) ───────┐
│  タイトル: 📎 請求書詳細: {invoiceNumber}        │
│  タブバー: [注文書一覧]                          │
│  注文書テーブル (4列) + アップロードボタン        │
└────────────────────────────────────────────────┘
```

**追加状態**:

| 状態 | 型 | 初期値 | 説明 |
|------|------|--------|------|
| selectedInvoice | InvoiceDTO\|null | null | 選択中の請求書（下層表示用） |
| documents[] | OrderDocument[] | [] | 注文書一覧 |
| docLoading | boolean | false | 注文書ローディング |

**行選択→注文書読込**:

```javascript
const handleRowClick = async (record) => {
  setSelectedInvoice(record);
  setDocLoading(true);
  try {
    const res = await api.get(`/invoice/${record.id}`);
    setDocuments(res.data.data.documents);
  } finally {
    setDocLoading(false);
  }
};
```

**テーブル行スタイル**: 選択行に `#e6f7ff` 背景色を適用し、選択状態を視覚化。

**請求書テーブルカラム (7列)**:

| カラムキー | dataIndex | render |
|-----------|-----------|--------|
| invoiceNumber | invoiceNumber | テキスト |
| customerName | customerName | テキスト |
| yearMonth | - | `${year}年${month}月` |
| amount | amount | 右寄せ + 3桁区切り |
| status | status | `<Tag>` (下書き=yellow, 送付済=blue, 入金済=green) |
| remark | remark | テキスト (長い場合省略) |
| actions | - | 編集/削除 |

**注文書テーブルカラム (4列)**:

| カラムキー | dataIndex | render |
|-----------|-----------|--------|
| fileName | fileName | 青文字テキスト |
| fileSize | fileSize | formatFileSize(bytes) → "245 KB" |
| createTime | createTime | dayjs().format('YYYY/MM/DD HH:mm') |
| actions | - | ダウンロード/削除 リンク |

**注文書アップロード**:

```javascript
const handleDocUpload = async (file) => {
  const formData = new FormData();
  formData.append('file', file);
  await api.post(`/invoice/${selectedInvoice.id}/documents`, formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  });
  await handleRowClick(selectedInvoice); // 注文書一覧再取得
  message.success('アップロードしました');
};
```

---

### 1.3 共通サービス設計

#### api.js — Axios インスタンス

```javascript
import axios from 'axios';

const api = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || 'http://localhost:8080/api',
  timeout: 30000,
  headers: { 'Content-Type': 'application/json' }
});

// リクエストインターセプター: JWTトークン付与
api.interceptors.request.use(config => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// レスポンスインターセプター: エラーハンドリング
api.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token');
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);

export default api;
```

#### AuthContext.jsx — 認証状態管理

```javascript
const AuthContext = createContext();

export const AuthProvider = ({ children }) => {
  const [user, setUser] = useState(null);
  const [token, setToken] = useState(localStorage.getItem('token'));

  const login = async (username, password) => {
    const res = await api.post('/auth/login', { username, password });
    const { token, username: uname, role } = res.data;
    localStorage.setItem('token', token);
    setToken(token);
    setUser({ username: uname, role });
  };

  const logout = () => {
    localStorage.removeItem('token');
    setToken(null);
    setUser(null);
  };

  // マウント時に /auth/me で既存トークン検証
  useEffect(() => {
    if (token) {
      api.get('/auth/me')
        .then(res => setUser({ username: res.data.data.username, role: res.data.data.role }))
        .catch(() => logout());
    }
  }, []);

  return (
    <AuthContext.Provider value={{ user, token, login, logout, isAuthenticated: !!token }}>
      {children}
    </AuthContext.Provider>
  );
};
```

#### usePagination.js — ページネーションフック

```javascript
export const usePagination = (initialPage = 0, initialSize = 10) => {
  const [pagination, setPagination] = useState({ page: initialPage, size: initialSize, total: 0 });

  const onChange = (page, pageSize) => setPagination(prev => ({
    ...prev, page: page - 1, size: pageSize  // Ant Design は 1-based
  }));

  const reset = () => setPagination({ page: 0, size: pagination.size, total: 0 });

  return { pagination, onChange, reset, setTotal: (t) => setPagination(prev => ({...prev, total: t})) };
};
```

---

### 1.4 国際化 (i18n) 設計

#### 翻訳キー命名規約

```
{画面カテゴリ}.{セクション}.{要素}

例:
- common.save             → 保存 / 保存
- common.cancel           → キャンセル / 取消
- common.edit             → 編集 / 编辑
- common.delete           → 削除 / 删除
- employee.title          → 社員管理 / 员工管理
- employee.searchCode     → 社員番号で検索 / 按员工编号搜索
- employee.field.code     → 社員番号 / 员工编号
- attendance.status.present → 出勤 / 出勤
- invoice.status.draft    → 下書き / 草稿
```

#### 翻訳ファイル構造

**ja.json** (日本語):

```json
{
  "common": {
    "save": "保存",
    "cancel": "キャンセル",
    "edit": "編集",
    "delete": "削除",
    "download": "ダウンロード",
    "upload": "アップロード",
    "search": "検索",
    "create": "新規登録",
    "logout": "ログアウト",
    "confirm": "確認",
    "yes": "はい",
    "no": "いいえ",
    "close": "閉じる"
  },
  "login": {
    "title": "ST管理システム",
    "subtitle": "社員・顧客・勤務・請求書の統合管理",
    "username": "ユーザー名",
    "password": "パスワード",
    "loginBtn": "ログイン",
    "error": "ユーザー名またはパスワードが間違っています。"
  },
  "employee": {
    "title": "社員管理",
    "searchCode": "社員番号で検索",
    "searchName": "氏名で検索",
    "field": {
      "code": "社員番号",
      "name": "氏名",
      "department": "部署",
      "email": "メールアドレス",
      "phone": "電話番号",
      "address": "住所",
      "position": "役職",
      "bankName": "銀行名",
      "bankBranch": "支店名",
      "bankAccountType": "口座種類",
      "bankAccountNumber": "口座番号",
      "bankAccountHolder": "口座名義",
      "attachment": "添付ファイル"
    },
    "section": {
      "personal": "個人情報",
      "bank": "銀行口座情報",
      "files": "添付ファイル"
    }
  },
  "customer": {
    "title": "顧客管理",
    "searchCode": "顧客番号で検索",
    "searchName": "会社名で検索",
    "field": {
      "code": "顧客番号",
      "company": "会社名",
      "contact": "担当者名",
      "email": "メールアドレス",
      "phone": "電話番号",
      "address": "住所"
    }
  },
  "attendance": {
    "title": "勤務管理",
    "field": {
      "employee": "社員",
      "date": "日付",
      "workHours": "勤務時間 (h)",
      "overtimeHours": "残業時間 (h)",
      "status": "ステータス",
      "remark": "備考"
    },
    "status": {
      "present": "出勤",
      "absent": "欠勤",
      "leave": "休暇"
    },
    "exportCsv": "CSVエクスポート",
    "allEmployees": "全社員"
  },
  "invoice": {
    "title": "請求書・注文書管理",
    "field": {
      "invoiceNumber": "請求書番号",
      "customer": "顧客名",
      "yearMonth": "年月",
      "amount": "金額 (円)",
      "status": "ステータス",
      "remark": "備考"
    },
    "status": {
      "draft": "下書き",
      "sent": "送付済",
      "paid": "入金済"
    },
    "detail": {
      "title": "請求書詳細",
      "documents": "注文書一覧",
      "uploadDoc": "注文書アップロード"
    }
  }
}
```

**zh.json** (中国語): 同等構造で中国語訳を提供。

---

## 2. バックエンドクラス詳細設計

### 2.1 JwtTokenProvider — JWTトークン管理

| 項目 | 内容 |
|------|------|
| パッケージ | `com.stmanagement.security` |

**メソッド一覧**:

| メソッド | 引数 | 戻り値 | 説明 |
|----------|------|--------|------|
| generateToken(username, role) | String, String | String | ユーザー名とロールを含むJWTを生成 (有効期限24h) |
| getUsernameFromToken(token) | String | String | トークンからユーザー名を抽出 |
| getRoleFromToken(token) | String | String | トークンからロールを抽出 |
| validateToken(token) | String | boolean | トークンの有効性検証 (署名＋有効期限) |

**JWTペイロード構造**:

```json
{
  "sub": "admin",
  "role": "ADMIN",
  "iat": 1685000000,
  "exp": 1685086400
}
```

**実装詳細**:

```java
@Component
public class JwtTokenProvider {
    @Value("${app.jwt.secret}")
    private String secret;

    @Value("${app.jwt.expiration-ms}")
    private long expirationMs;

    private SecretKey getSigningKey() {
        return Keys.hmacShaKeyFor(Decoders.BASE64.decode(
            Base64.getEncoder().encodeToString(secret.getBytes(StandardCharsets.UTF_8))
        ));
    }

    public String generateToken(String username, String role) {
        Date now = new Date();
        Date expiry = new Date(now.getTime() + expirationMs);
        return Jwts.builder()
            .subject(username)
            .claim("role", role)
            .issuedAt(now)
            .expiration(expiry)
            .signWith(getSigningKey())
            .compact();
    }
    // ... 他のメソッド
}
```

---

### 2.2 JwtAuthFilter — 認証フィルター

| 項目 | 内容 |
|------|------|
| 継承 | `OncePerRequestFilter` |

**フィルターフロー**:

```
HTTPリクエスト
  │
  ├─ Authorization ヘッダーあり？
  │    │
  │    ├─ NO → filterChain.doFilter() (認証なしで通過)
  │    │
  │    └─ YES → "Bearer " プレフィックス確認
  │              │
  │              ├─ 形式不正 → 401 Unauthorized
  │              │
  │              └─ OK → JWT検証
  │                       │
  │                       ├─ 検証失敗 → 401 Unauthorized
  │                       │
  │                       └─ OK → SecurityContextHolder に認証情報設定
  │                                → filterChain.doFilter()
```

**スキップ対象** (SecurityConfig で設定):
- `POST /api/auth/login`
- `OPTIONS /**`

---

### 2.3 FileStorageService — ファイル保存サービス

| 項目 | 内容 |
|------|------|
| パッケージ | `com.stmanagement.service` |

**メソッド一覧**:

| メソッド | 引数 | 戻り値 | 説明 |
|----------|------|--------|------|
| store(file, subDir) | MultipartFile, String | String | ファイルを保存し、格納パスを返す |
| load(filePath) | String | Path | ファイルパスからPathオブジェクト取得 |
| delete(filePath) | String | void | ファイル削除 |

**ファイル保存ロジック**:

```java
public String store(MultipartFile file, String subDir) {
    // 1. 元ファイル名から拡張子を抽出
    String originalName = file.getOriginalFilename();
    String ext = originalName.substring(originalName.lastIndexOf('.'));
    // 2. UUIDで一意ファイル名を生成
    String storedName = UUID.randomUUID().toString() + ext;
    // 3. 保存先ディレクトリを確定 (./uploads/{subDir}/)
    Path dir = Paths.get(uploadDir, subDir);
    Files.createDirectories(dir);
    // 4. ファイル保存
    Path target = dir.resolve(storedName);
    Files.copy(file.getInputStream(), target, StandardCopyOption.REPLACE_EXISTING);
    // 5. 相対パスを返却
    return subDir + "/" + storedName;
}
```

**アップロード許可拡張子**:

```java
private static final Set<String> ALLOWED_EXTENSIONS = Set.of(
    ".pdf", ".doc", ".docx", ".xls", ".xlsx", ".jpg", ".jpeg", ".png"
);
```

---

### 2.4 GlobalExceptionHandler — 例外ハンドラー

| 項目 | 内容 |
|------|------|
| アノテーション | `@ControllerAdvice` |

**例外 → HTTPステータス マッピング**:

| 例外クラス | HTTPステータス | レスポンスボディ |
|-----------|:------------:|-----------------|
| ResourceNotFoundException | 404 | {error: "リソースが見つかりません", status: 404} |
| BadRequestException | 400 | {error: message, status: 400} |
| MethodArgumentNotValidException | 400 | {error: "バリデーションエラー: フィールド名", status: 400} |
| DataIntegrityViolationException | 400 | {error: "一意制約違反", status: 400} |
| Exception (汎用) | 500 | {error: "サーバー内部エラー", status: 500} |

---

## 3. DTO 設計

### 3.1 EmployeeDTO

```java
public class EmployeeDTO {
    private Long id;
    @NotBlank private String employeeCode;
    @NotBlank private String name;
    @Email private String email;
    private String phone;
    private String address;
    private String department;
    private String position;
    private String bankName;
    private String bankBranch;
    private String bankAccountType;
    private String bankAccountNumber;
    private String bankAccountHolder;
    private String attachmentPath;
    private LocalDateTime createTime;
    private LocalDateTime updateTime;
}
```

### 3.2 AttendanceDTO

```java
public class AttendanceDTO {
    private Long id;
    @NotNull private Long employeeId;
    private String employeeName;  // JOIN取得、表示用
    @NotNull private LocalDate workDate;
    @DecimalMin("0.0") @DecimalMax("24.0") private BigDecimal workHours;
    @DecimalMin("0.0") @DecimalMax("12.0") private BigDecimal overtimeHours;
    @NotBlank private String status;
    private String remark;
}
```

### 3.3 InvoiceDTO

```java
public class InvoiceDTO {
    private Long id;
    @NotBlank private String invoiceNumber;
    @NotNull private Long customerId;
    private String customerName;  // JOIN取得、表示用
    @NotNull private Integer year;
    @NotNull private Integer month;
    @NotNull private BigDecimal amount;
    @NotBlank private String status;
    private String remark;
    private List<OrderDocumentDTO> documents;  // 詳細取得時のみ
}
```

### 3.4 ApiResponse<T> (共通レスポンス)

```java
public class ApiResponse<T> {
    private T data;
    private String message;

    public static <T> ApiResponse<T> success(T data) {
        return new ApiResponse<>(data, "success");
    }
    public static <T> ApiResponse<T> success(T data, String message) {
        return new ApiResponse<>(data, message);
    }
}
```
